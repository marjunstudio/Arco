@AGENTS.md

## Claude Code

- ドキュメントと実装のズレを見つけたときの判定・修正手順は `doc-drift` skill にある（`.claude/skills/doc-drift/`）
- モジュール分割、センサー層の抽象、タブバーの OS 分岐に手を入れる作業はプランモードで方針を出してから着手する
- 実機が必要な確認（歩数・方位・触覚・屋外での歩行）は自分では実行できない。ビルドとテストまでを通し、実機確認が必要な項目は明示してユーザーに渡す
